name: Projekt-Check – Aktuellen Testfall Blockchain/DeFi prüfen

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/projekt-check-backfill-blockchain-current.yml'

permissions:
  contents: write

jobs:
  blockchain-current:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Chromium bereitstellen
        run: |
          python -m pip install --disable-pip-version-check playwright
          python -m playwright install --with-deps chromium
      - name: U-Center Blockchain, Wallets, Contracts und DeFi technisch prüfen
        run: python projekt-check-engine/core/run_blockchain_depth.py --case-id PCA-20260825-NN0U4DO7 --max-captures 8
      - name: Technischen Teststand speichern
        run: |
          git config user.name "FSA Projekt-Check"
          git config user.email "actions@github.com"
          git add data/projekt-check/cases/PCA-20260825-NN0U4DO7
          git commit -m "Projekt-Check: U-Center Blockchain-DeFi-Tiefenprüfung"
          git pull --rebase origin main
          git push origin HEAD:main
