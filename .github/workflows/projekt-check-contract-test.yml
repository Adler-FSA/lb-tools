name: Projekt-Check – Contract Test

on:
  push:
    branches: [main]
    paths:
      - 'projekt-check-engine/**'
      - 'projekt-check-auswertung/**'
      - '.github/workflows/projekt-check-contract-test.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Python Contract Tests
        run: python -m unittest discover -s projekt-check-engine/tests -p 'test_*.py'
