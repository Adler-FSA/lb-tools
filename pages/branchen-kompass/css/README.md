# Gemeinsames Design

Die Startseite enthält das aus den Referenzseiten abgeleitete Design zunächst vollständig und eigenständig im HTML (`../index.html`). Erst mit der ersten fachlich abgenommenen Simulatorseite werden hier wirklich benötigte gemeinsame CSS-Bestandteile abgelegt. Keine Änderungen an freigegebenen Hoteldateien.
