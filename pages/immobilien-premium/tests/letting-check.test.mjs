import test from 'node:test';
import assert from 'node:assert/strict';
import {createLettingProcess,advanceLettingPhase,setLettingItemState,deleteLettingProcess} from '../assets/js/letting-check.js';
function fixture(){return {properties:[{id:'p1'}],units:[{id:'u1',propertyId:'p1'}],checkItems:[],usagePeriods:[],tenancies:[],contractTerms:[],accountingPeriods:[],expenses:[],allocationRules:[],meters:[],readings:[],cashflows:[],documents:[],attachments:[]};}
test('Vermietungscheck startet datensparsam ohne Bewerberantworten und Score',()=>{const r=createLettingProcess(fixture(),{processId:'v1',propertyId:'p1',unitId:'u1',createdOn:'2026-09-24'});const x=r.project.checkItems[0];assert.equal(x.phase,'A_VIEWING');assert.equal(x.applicantAnswersStored,false);assert.equal(x.evidenceFilesStored,false);assert.equal(x.automaticScore,false);assert.equal(x.automaticSelection,false);});
test('Spätere Prüffragen sind vor ihrer Phase gesperrt',()=>{const p=createLettingProcess(fixture(),{processId:'v1',propertyId:'p1',unitId:'u1',createdOn:'2026-09-24'}).project;assert.throws(()=>setLettingItemState(p,{processId:'v1',itemKey:'c_income_evidence',state:'done'}),/späteren/);});
test('Phasen laufen nur A nach B nach C',()=>{let p=createLettingProcess(fixture(),{processId:'v1',propertyId:'p1',unitId:'u1',createdOn:'2026-09-24'}).project;assert.throws(()=>advanceLettingPhase(p,{processId:'v1',nextPhase:'C_SELECTED'}),/Schritt/);p=advanceLettingPhase(p,{processId:'v1',nextPhase:'B_INTEREST'}).project;p=advanceLettingPhase(p,{processId:'v1',nextPhase:'C_SELECTED'}).project;assert.equal(p.checkItems[0].phase,'C_SELECTED');});
test('Vermietungsvorgang ist vollständig löschbar',()=>{let p=createLettingProcess(fixture(),{processId:'v1',propertyId:'p1',unitId:'u1',createdOn:'2026-09-24'}).project;p=deleteLettingProcess(p,{processId:'v1'}).project;assert.equal(p.checkItems.length,0);});
